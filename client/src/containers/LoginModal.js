import React from "react";
import ModalLayout from "../components/ModalLayout";

const LoginModal = ({ open, setOpen }) => {
  return <ModalLayout open={open} setOpen={setOpen}/>;
};

export default LoginModal;
